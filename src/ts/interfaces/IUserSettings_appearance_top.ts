export interface IUserSettings_appearance_top {
    /**
     * Checks if 'add note' button should be displayed on top
     */
    addNote: boolean;
    /**
     * Checks if 'add category' button should be displayed on top
     */
    addCategory: boolean;
}
