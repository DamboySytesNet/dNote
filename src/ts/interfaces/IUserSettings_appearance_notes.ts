export interface IUserSettings_appearance_notes {
    /**
     * Check if note additions are shown
     */
    showTop: boolean;
    /**
     * Check if note content is shown
     */
    showText: boolean;
    /**
     * Check if note tags are shown
     */
    showTags: boolean;
}
